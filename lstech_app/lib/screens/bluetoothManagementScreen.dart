import 'package:flutter/material.dart';

class BluetoothManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bluetooth Management"),
      ),
      body: Center(
        child: Text("add logic here"),
      ),
    );
  }
}
